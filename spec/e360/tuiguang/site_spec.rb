require "spec_helper"

describe E360::Tuiguang::Site do
  it "has a version number" do
    expect(E360::Tuiguang::Site::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
